<!--index.wxml-->
<view class="page">
    <After-Login wx:if="{{isLogin}}" />
    <Before-Login wx:else />
    <List />
    <view class="weui-footer footer">
        <view class="weui-footer__text">加入后才能使用全部功能哟~</view>
    </view>
</view>